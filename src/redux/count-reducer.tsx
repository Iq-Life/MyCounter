




export type initialStateType ={
    count: number
}

let initialState = {
    count: 0
};
const countReducer = (state = initialState, action: ActionType) : initialStateType => {
    switch (action.type) {
        case ADD_COUNT
    }
}